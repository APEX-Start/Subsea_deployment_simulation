# Subsea Deployment Simulation 仓库整理清单与流程

本文档用于指导你手动整理仓库：

```text
D:\Desktop\subea-open\Subsea_deployment_simulation
```

可视化源码来源目录：

```text
D:\Desktop\RBDO1\subsea_deploy_optimization\subsea_edeploy_optimization\bayesian_optimization\multi_optimization\SCI_paper
```

目标不是一次性大改算法，而是把现有研究代码整理成“别人可以克隆、安装、运行、复现实验、生成论文图”的项目结构。

---

## 0. 总原则

整理时遵守下面几条原则：

- 先保证现有代码还能运行，再改结构。
- 每完成一个阶段就运行检查，并提交一次 Git commit。
- 源码、脚本、笔记本、结果数据、论文图、说明文档分开管理。
- 不把大型仿真结果、Excel、NPZ、SVG 成图混在源码根目录。
- 可视化脚本可以进仓库，仿真生成的数据包原则上不进普通 Git，除非确认体积小且必须复现实验。
- 任何移动文件操作都用 `git mv`，这样 Git 能识别重命名，历史更清楚。
- 仿真模型的物理逻辑不要和整理仓库同时重构，避免定位问题困难。

---

## 1. 当前仓库状态检查

当前仓库根目录大致是：

```text
Subsea_deployment_simulation/
├── .git/
├── .gitignore
├── README.md
├── requirements.txt
├── high_fidelity_sim.py
├── convergence_analysis.py
├── sensitivity_analysis.py
└── sea_current.ipynb
```

当前特点：

- 根目录直接放了主仿真脚本、分析脚本和 notebook。
- `README.md` 和 `requirements.txt` 已经存在，可以保留并更新。
- `.gitignore` 已经存在，可以补充更细的输出目录规则。
- `high_fidelity_sim.py` 是核心仿真模型。
- `convergence_analysis.py` 是收敛性和静态悬链线验证脚本。
- `sensitivity_analysis.py` 是 LHS 敏感性分析脚本。
- `sea_current.ipynb` 是海流 notebook。
- 可视化相关代码目前不在仓库内，而是在外部 `SCI_paper` 目录。

整理前先运行：

```powershell
cd D:\Desktop\subea-open\Subsea_deployment_simulation
git status --short --branch
git remote -v
python -m py_compile high_fidelity_sim.py convergence_analysis.py sensitivity_analysis.py
```

预期：

- `git status` 显示当前在 `main` 分支，工作区干净。
- `py_compile` 不报错。

如果工作区不干净，先不要整理。先确认哪些文件是你想保留的修改。

---

## 2. 推荐目标结构

建议整理成下面结构：

```text
Subsea_deployment_simulation/
├── README.md
├── LICENSE
├── pyproject.toml
├── requirements.txt
├── .gitignore
├── 整理清单与流程.md
├── src/
│   └── subsea_deployment_simulation/
│       ├── __init__.py
│       ├── high_fidelity_sim.py
│       └── plotting_style.py
├── scripts/
│   ├── convergence_analysis.py
│   ├── sensitivity_analysis.py
│   └── visualization/
│       ├── plot_01_upper_boundary_heave_disturbance.py
│       ├── plot_02_payload_vertical_coordinate_history.py
│       ├── plot_03_wire_rope_snapshot_envelope.py
│       ├── plot_04_pch_deployment_trajectory.py
│       ├── plot_05_pch_and_tool_tilt_histories.py
│       ├── plot_06_final_hover_stage_zoom_in.py
│       ├── plot_07_top_tension_history.py
│       ├── plot_08_maximum_tension_stress_envelope.py
│       ├── plot_09_wire_rope_stress_time_history.py
│       ├── plot_10_payload_trajectory_3d_and_displacements.py
│       ├── plot_11_operational_window.py
│       ├── plot_12_sea_current.py
│       └── sweep_operational_window.py
├── notebooks/
│   └── sea_current.ipynb
├── data/
│   ├── README.md
│   └── .gitkeep
├── results/
│   └── .gitkeep
├── docs/
│   ├── usage.md
│   ├── visualization.md
│   └── reproduction.md
└── tests/
    └── test_imports.py
```

各目录职责：

- `src/subsea_deployment_simulation/`：可导入的 Python 包，放核心模型和通用工具。
- `scripts/`：命令行脚本，负责调用包里的模型，不放大量核心类定义。
- `scripts/visualization/`：论文图和可视化脚本。
- `notebooks/`：Jupyter notebook。
- `data/`：小型示例数据、数据说明。大型数据不要直接提交。
- `results/`：运行输出目录，仅保留 `.gitkeep`，实际生成结果不提交。
- `docs/`：补充说明文档。
- `tests/`：最小自动化检查。

---

## 3. 第一步：建立整理分支

不要直接在 `main` 上整理。先建一个分支：

```powershell
cd D:\Desktop\subea-open\Subsea_deployment_simulation
git checkout -b repo-cleanup
```

确认：

```powershell
git status --short --branch
```

预期看到：

```text
## repo-cleanup
```

---

## 4. 第二步：创建目录

执行：

```powershell
mkdir src
mkdir src\subsea_deployment_simulation
mkdir scripts
mkdir scripts\visualization
mkdir notebooks
mkdir data
mkdir results
mkdir docs
mkdir tests
```

保留空目录需要 `.gitkeep`：

```powershell
New-Item -ItemType File data\.gitkeep
New-Item -ItemType File results\.gitkeep
```

---

## 5. 第三步：移动核心文件

用 `git mv` 移动文件：

```powershell
git mv high_fidelity_sim.py src\subsea_deployment_simulation\high_fidelity_sim.py
git mv convergence_analysis.py scripts\convergence_analysis.py
git mv sensitivity_analysis.py scripts\sensitivity_analysis.py
git mv sea_current.ipynb notebooks\sea_current.ipynb
```

新增包入口文件：

```powershell
New-Item -ItemType File src\subsea_deployment_simulation\__init__.py
```

建议 `src/subsea_deployment_simulation/__init__.py` 内容：

```python
"""Subsea deployment simulation package."""

from .high_fidelity_sim import (
    DEFAULT_CONFIG,
    DynamicWireRopeSystem3D,
    VesselMotion,
    WireRopeSystem3D,
)

__all__ = [
    "DEFAULT_CONFIG",
    "DynamicWireRopeSystem3D",
    "VesselMotion",
    "WireRopeSystem3D",
]
```

注意：

- 如果 `high_fidelity_sim.py` 里没有某个名字，就不要放进 `__init__.py`。
- 移动后，原来的 `python high_fidelity_sim.py` 入口会变成：

```powershell
python -m subsea_deployment_simulation.high_fidelity_sim
```

但要先完成 `pyproject.toml` 或设置 `PYTHONPATH`。

---

## 6. 第四步：新增 pyproject.toml

新增 `pyproject.toml`，让项目可以用 editable 模式安装：

```toml
[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "subsea-deployment-simulation"
version = "0.1.0"
description = "High-fidelity simulation and analysis tools for subsea deployment dynamics."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
  "numpy>=1.20.0",
  "scipy>=1.7.0",
  "pandas>=1.3.0",
  "matplotlib>=3.4.0",
  "seaborn>=0.11.0",
  "scikit-learn",
  "openpyxl",
  "jupyter>=1.0.0",
]

[tool.setuptools.packages.find]
where = ["src"]
```

然后安装：

```powershell
python -m pip install --upgrade pip
python -m pip install -e .
```

验证包导入：

```powershell
python -c "import subsea_deployment_simulation as s; print(s.DynamicWireRopeSystem3D.__name__)"
```

---

## 7. 第五步：修正分析脚本导入

移动后，`scripts/convergence_analysis.py` 和 `scripts/sensitivity_analysis.py` 不能再直接写：

```python
from high_fidelity_sim import ...
```

推荐改成：

```python
from subsea_deployment_simulation.high_fidelity_sim import (
    DEFAULT_CONFIG as HIGH_FIDELITY_DEFAULT_CONFIG,
    DynamicWireRopeSystem3D,
    WireRopeSystem3D,
)
```

如果脚本需要从源码树直接运行，而用户没有执行 `pip install -e .`，可以在脚本顶部保留兼容路径：

```python
from pathlib import Path
import sys

PROJECT_ROOT = Path(__file__).resolve().parents[1]
SRC_ROOT = PROJECT_ROOT / "src"
if str(SRC_ROOT) not in sys.path:
    sys.path.insert(0, str(SRC_ROOT))
```

然后再导入包：

```python
from subsea_deployment_simulation.high_fidelity_sim import DynamicWireRopeSystem3D
```

注意：

- `convergence_analysis.py` 当前有一个 `setup_publication_style()`，调用处是 `setup_publication_style(show_message=False)`，这会导致参数不匹配风险。
- 你需要二选一：
  - 把函数定义改成 `def setup_publication_style(show_message: bool = True):`
  - 或把调用改成 `setup_publication_style()`
- 更干净的做法是把样式函数放进 `src/subsea_deployment_simulation/plotting_style.py`，两个分析脚本和可视化脚本都从这里导入。

---

## 8. 第六步：整理绘图样式函数

当前可视化源码里有：

```text
SCI_paper\plot_style.py
```

建议迁移为：

```text
src\subsea_deployment_simulation\plotting_style.py
```

迁移后，所有绘图脚本统一改成：

```python
from subsea_deployment_simulation.plotting_style import apply_times_new_roman_style
```

如果分析脚本使用的是 `setup_publication_style()`，可以在 `plotting_style.py` 中同时提供：

```python
def setup_publication_style(show_message: bool = True):
    ...

def apply_times_new_roman_style():
    ...
```

这样可以避免每个脚本复制一份样式配置。

---

## 9. 第七步：迁移 SCI_paper 可视化源码

外部可视化目录：

```text
D:\Desktop\RBDO1\subsea_deploy_optimization\subsea_edeploy_optimization\bayesian_optimization\multi_optimization\SCI_paper
```

里面主要有几类文件：

### 9.1 应该迁入仓库的源码

建议迁入：

```text
plot_01_upper_boundary_heave_disturbance.py
plot_02_payload_vertical_coordinate_history.py
plot_03_wire_rope_snapshot_envelope.py
plot_04_pch_deployment_trajectory.py
plot_05_pch_and_tool_tilt_histories.py
plot_06_final_hover_stage_zoom_in.py
plot_07_top_tension_history.py
plot_08_maximum_tension_stress_envelope.py
plot_09_wire_rope_stress_time_history.py
plot_10_payload_trajectory_3d_and_displacements.py
plot_11_operational_window.py
sea_current.py
sweep_operational_window.py
plot_style.py
```

目标位置：

```text
scripts\visualization\
```

其中 `plot_style.py` 最好不要放在 `scripts/visualization/`，而是迁移到：

```text
src\subsea_deployment_simulation\plotting_style.py
```

复制命令示例：

```powershell
$sci = "D:\Desktop\RBDO1\subsea_deploy_optimization\subsea_edeploy_optimization\bayesian_optimization\multi_optimization\SCI_paper"
Copy-Item "$sci\plot_*.py" scripts\visualization\
Copy-Item "$sci\sea_current.py" scripts\visualization\plot_12_sea_current.py
Copy-Item "$sci\sweep_operational_window.py" scripts\visualization\
Copy-Item "$sci\plot_style.py" src\subsea_deployment_simulation\plotting_style.py
```

### 9.2 不建议直接提交的生成文件

这些文件通常是运行结果或大型数据，不建议直接提交到普通 Git：

```text
visualization_payload.npz
simulation_data_original.xlsx
sweep_results.npz
fig_01_upper_boundary_heave_disturbance.svg
fig_02_payload_vertical_coordinate_history.svg
fig_03_wire_rope_snapshot_envelope.svg
fig_04_pch_deployment_trajectory.svg
fig_05_pch_and_tool_tilt_histories.svg
fig_06_final_hover_stage_zoom_in.svg
fig_07_top_tension_history.svg
fig_08_maximum_tension_stress_envelope.svg
fig_09_wire_rope_stress_time_history.svg
fig_10_payload_trajectory_3d_and_displacements.svg
fig_11_operational_window.svg
plot_12_sea_current.svg
```

原因：

- `visualization_payload.npz` 约数 MB，未来可能继续变大。
- `simulation_data_original.xlsx` 是仿真输出，不是源码。
- SVG 成图是输出物，应该由脚本生成。
- 结果文件频繁变化，会让 Git 历史很乱。

建议策略：

- 仓库提交可视化源码。
- 仓库不提交大结果文件。
- 在 `docs/visualization.md` 说明如何由仿真生成数据包，再运行绘图脚本生成 SVG。
- 如果论文必须固定某组图，可以考虑单独建 `paper-assets/`，只提交最终精选图片，并说明来源。

### 9.3 如果必须保存大文件

如果你确实需要把 `visualization_payload.npz`、`simulation_data_original.xlsx` 等文件纳入版本管理，建议使用 Git LFS：

```powershell
git lfs install
git lfs track "*.npz"
git lfs track "*.xlsx"
git add .gitattributes
```

但不建议默认这么做。GitHub 免费额度和协作者下载成本都要考虑。

---

## 10. 第八步：修正可视化脚本的数据路径

目前可视化脚本普遍会寻找：

```text
SCI_paper\visualization_payload.npz
visualization_payload.npz
SCI_paper\sweep_results.npz
```

整理后建议统一数据路径：

```text
results\visualization\visualization_payload.npz
results\visualization\sweep_results.npz
results\figures\
```

推荐让每个脚本支持命令行参数：

```powershell
python scripts\visualization\plot_01_upper_boundary_heave_disturbance.py --input results\visualization\visualization_payload.npz --output results\figures
```

如果暂时不想加 argparse，也至少让脚本按以下顺序查找数据：

```python
candidates = [
    PROJECT_ROOT / "results" / "visualization" / "visualization_payload.npz",
    PROJECT_ROOT / "SCI_paper" / "visualization_payload.npz",
    Path.cwd() / "visualization_payload.npz",
]
```

输出图建议统一写到：

```text
results\figures\
```

不要写回 `scripts/visualization/`。

---

## 11. 第九步：更新 .gitignore

建议 `.gitignore` 至少包含：

```gitignore
# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd

# Environments
.env
.venv/
env/
venv/
ENV/

# Jupyter
.ipynb_checkpoints/

# Build
build/
dist/
*.egg-info/

# Outputs
results/**
!results/
!results/.gitkeep
SCI_paper/

# Generated data and figures
*.csv
*.xlsx
*.npz
*.png
*.jpg
*.jpeg
*.svg
*.pdf

# IDE
.vscode/
.idea/
*.swp
```

注意：

- 如果你决定提交最终 SVG 成图，就不要全局忽略 `*.svg`，改成只忽略 `results/**/*.svg`。
- 如果你决定提交小型 CSV 示例数据，就不要全局忽略 `*.csv`，改成只忽略 `results/**/*.csv`。

更推荐的精细规则是：

```gitignore
results/**
!results/
!results/.gitkeep

SCI_paper/
*.npz
*.xlsx
__pycache__/
*.py[cod]
.ipynb_checkpoints/
```

---

## 12. 第十步：更新 requirements.txt

当前 `requirements.txt` 已有：

```text
numpy
scipy
pandas
matplotlib
seaborn
jupyter
notebook
```

根据现有脚本，还需要确认：

- `scikit-learn`：`sensitivity_analysis.py` 使用 `sklearn.linear_model.LinearRegression`
- `openpyxl`：如果 `pandas` 写入或读取 `.xlsx`，通常需要

建议改成：

```text
numpy>=1.20.0
scipy>=1.7.0
pandas>=1.3.0
matplotlib>=3.4.0
seaborn>=0.11.0
scikit-learn>=1.0.0
openpyxl>=3.0.0
jupyter>=1.0.0
notebook>=6.4.0
```

如果你新增 `pyproject.toml`，保持 `requirements.txt` 和 `pyproject.toml` 依赖一致。

---

## 13. 第十一步：更新 README.md

README 需要从“文件都在根目录”改成“包结构项目”。

建议包含：

### 13.1 项目简介

说明项目做什么：

- 三维钢丝绳水下放物系统高保真仿真
- 动态下放过程
- 母船运动、波浪、海流、钢丝绳动力学、载荷和应力
- 敏感性分析
- 收敛性验证
- 论文图可视化脚本

### 13.2 目录结构

把目标结构贴出来。

### 13.3 安装

```powershell
git clone https://github.com/APEX-Start/Subsea_deployment_simulation.git
cd Subsea_deployment_simulation
python -m venv .venv
.\.venv\Scripts\activate
python -m pip install --upgrade pip
python -m pip install -e .
```

### 13.4 运行主仿真

```powershell
python -m subsea_deployment_simulation.high_fidelity_sim
```

说明输出会生成到：

```text
SCI_paper\
```

或整理后建议改成：

```text
results\simulation\
results\visualization\
```

### 13.5 运行分析

```powershell
python scripts\convergence_analysis.py --mode catenary
python scripts\convergence_analysis.py --mode mesh --quick
python scripts\sensitivity_analysis.py --fig20-only
```

如果补了 `--samples` 和 `--quick`：

```powershell
python scripts\sensitivity_analysis.py --quick
python scripts\sensitivity_analysis.py --samples 20
```

### 13.6 运行可视化

```powershell
python scripts\visualization\plot_01_upper_boundary_heave_disturbance.py
python scripts\visualization\plot_02_payload_vertical_coordinate_history.py
python scripts\visualization\plot_11_operational_window.py
```

说明需要先有：

```text
results\visualization\visualization_payload.npz
results\visualization\sweep_results.npz
```

---

## 14. 第十二步：新增 docs 文档

建议新增：

```text
docs\usage.md
docs\visualization.md
docs\reproduction.md
```

### 14.1 docs/usage.md

写清楚：

- 如何创建环境
- 如何安装依赖
- 如何运行主仿真
- 常见参数在哪里改
- 输出文件在哪里

### 14.2 docs/visualization.md

写清楚：

- 可视化脚本来源于外部 `SCI_paper` 目录
- 每个 `plot_XX` 脚本生成哪张图
- 依赖哪些输入数据
- 输出到哪个目录

建议表格：

```text
脚本 | 输入 | 输出 | 说明
plot_01...py | visualization_payload.npz | fig_01...svg | 上边界升沉扰动
plot_02...py | visualization_payload.npz | fig_02...svg | 载荷垂向坐标
...
plot_11...py | sweep_results.npz | fig_11...svg | 作业窗口
plot_12...py | 内置海流参数 | plot_12_sea_current.svg | 海流剖面
```

### 14.3 docs/reproduction.md

写清楚从零复现实验的顺序：

1. 安装环境。
2. 运行主仿真生成 `visualization_payload.npz`。
3. 运行作业窗口 sweep 生成 `sweep_results.npz`。
4. 运行 `plot_01` 到 `plot_12`。
5. 运行收敛性分析。
6. 运行敏感性分析。

---

## 15. 第十三步：补最小测试

新增：

```text
tests\test_imports.py
```

内容：

```python
def test_core_imports():
    from subsea_deployment_simulation.high_fidelity_sim import (
        DEFAULT_CONFIG,
        DynamicWireRopeSystem3D,
        WireRopeSystem3D,
    )

    assert isinstance(DEFAULT_CONFIG, dict)
    assert DynamicWireRopeSystem3D is not None
    assert WireRopeSystem3D is not None
```

安装 pytest：

```powershell
python -m pip install pytest
```

运行：

```powershell
python -m pytest
```

如果暂时不想加 pytest，至少保留编译检查：

```powershell
python -m py_compile src\subsea_deployment_simulation\high_fidelity_sim.py scripts\convergence_analysis.py scripts\sensitivity_analysis.py
```

---

## 16. 第十四步：每阶段验证命令

整理过程建议分阶段验证。

### 16.1 移动文件后

```powershell
git status --short
python -m py_compile src\subsea_deployment_simulation\high_fidelity_sim.py scripts\convergence_analysis.py scripts\sensitivity_analysis.py
```

### 16.2 安装包后

```powershell
python -m pip install -e .
python -c "import subsea_deployment_simulation; print('import ok')"
```

### 16.3 分析脚本入口检查

```powershell
python scripts\convergence_analysis.py --help
python scripts\sensitivity_analysis.py --help
```

### 16.4 可视化脚本入口检查

先不要跑所有图。先检查导入：

```powershell
python -m py_compile scripts\visualization\plot_01_upper_boundary_heave_disturbance.py
python -m py_compile scripts\visualization\plot_11_operational_window.py
python -m py_compile scripts\visualization\plot_12_sea_current.py
```

然后准备数据：

```powershell
mkdir results\visualization
```

如果你只是本地验证，可以临时复制：

```powershell
$sci = "D:\Desktop\RBDO1\subsea_deploy_optimization\subsea_edeploy_optimization\bayesian_optimization\multi_optimization\SCI_paper"
Copy-Item "$sci\visualization_payload.npz" results\visualization\
Copy-Item "$sci\sweep_results.npz" results\visualization\
```

注意：复制到 `results/` 后不要提交这些文件。

### 16.5 轻量运行

```powershell
python scripts\convergence_analysis.py --mode catenary --relax-time 10 --n-eval 20
```

如果这个也太慢，就只运行 `--help` 和 `py_compile`。

---

## 17. 第十五步：推荐提交顺序

不要把所有整理一次性提交。建议分 5 个 commit：

### Commit 1：新增项目结构

内容：

- 新增 `src/`
- 新增 `scripts/`
- 新增 `notebooks/`
- 新增 `data/`
- 新增 `results/`
- 移动三个 `.py` 和 notebook
- 新增 `__init__.py`

命令：

```powershell
git add .
git commit -m "Organize repository layout"
```

### Commit 2：修正导入和包配置

内容：

- 新增 `pyproject.toml`
- 修正脚本导入路径
- 更新 `requirements.txt`

命令：

```powershell
git add .
git commit -m "Add package configuration and update imports"
```

### Commit 3：迁移可视化源码

内容：

- 复制 `SCI_paper` 里的绘图脚本
- 迁移 `plot_style.py`
- 修正可视化脚本导入
- 修正数据路径

命令：

```powershell
git add .
git commit -m "Add visualization scripts"
```

### Commit 4：更新文档

内容：

- 更新 `README.md`
- 新增 `docs/usage.md`
- 新增 `docs/visualization.md`
- 新增 `docs/reproduction.md`
- 保留本清单

命令：

```powershell
git add .
git commit -m "Document usage and visualization workflow"
```

### Commit 5：补测试和忽略规则

内容：

- 更新 `.gitignore`
- 新增 `tests/test_imports.py`
- 清理误入的输出文件

命令：

```powershell
git add .
git commit -m "Add basic checks and cleanup ignore rules"
```

---

## 18. 第十六步：推送前检查

推送前运行：

```powershell
git status --short
git log --oneline -5
python -m py_compile src\subsea_deployment_simulation\high_fidelity_sim.py scripts\convergence_analysis.py scripts\sensitivity_analysis.py
python scripts\convergence_analysis.py --help
python scripts\sensitivity_analysis.py --help
```

检查是否误提交大文件：

```powershell
git ls-files | Select-String -Pattern "\.npz$|\.xlsx$|\.png$|\.jpg$|\.jpeg$|\.svg$|\.pdf$"
```

如果输出里有你不想提交的结果文件，移除缓存但保留本地文件：

```powershell
git rm --cached path\to\file
```

推送分支：

```powershell
git push -u origin repo-cleanup
```

然后在 GitHub 上开 Pull Request，把整理内容合并到 `main`。

如果你确认要直接推 main：

```powershell
git checkout main
git merge repo-cleanup
git push origin main
```

更推荐开 PR，因为整理仓库容易漏路径或误提交结果文件。

---

## 19. 风险清单

整理时重点注意这些风险：

- `setup_publication_style(show_message=False)` 和函数定义参数不一致。
- 移动 `high_fidelity_sim.py` 后，脚本中 `from high_fidelity_sim import ...` 会失效。
- 可视化脚本依赖 `plot_style.py`，迁移后必须统一导入路径。
- 可视化脚本依赖 `visualization_payload.npz` 和 `sweep_results.npz`，不要误删本地数据。
- `.gitignore` 如果全局忽略 `*.svg`，最终论文图也会被忽略。
- `.gitignore` 如果全局忽略 `*.csv`，示例数据也会被忽略。
- notebook 里可能存在旧路径，需要打开检查。
- `README.md` 里的运行命令必须和新目录结构一致。
- 如果没有 `pip install -e .`，`python -m subsea_deployment_simulation.high_fidelity_sim` 可能找不到包。
- 如果用 Git LFS，协作者也需要安装 Git LFS。

---

## 20. 最终验收标准

整理完成后，仓库应满足：

- 克隆后能按 README 安装环境。
- `python -m py_compile ...` 通过。
- `python scripts\convergence_analysis.py --help` 通过。
- `python scripts\sensitivity_analysis.py --help` 通过。
- 核心包可以导入：

```powershell
python -c "import subsea_deployment_simulation; print('ok')"
```

- 根目录不再堆放 `.py` 主脚本。
- 根目录不再堆放 `.ipynb`。
- 根目录不再堆放生成图、Excel、NPZ。
- 可视化源码已经进入 `scripts\visualization\`。
- 可视化输入输出路径写清楚。
- README 和 docs 里的命令能对应真实文件路径。
- Git 历史里没有误提交大体积结果文件。

---

## 21. 推荐执行顺序摘要

如果只想照着最短路线做，按这个顺序：

```text
1. git checkout -b repo-cleanup
2. 创建 src/scripts/notebooks/data/results/docs/tests
3. git mv 核心脚本和 notebook
4. 新增 __init__.py 和 pyproject.toml
5. 修正 convergence_analysis.py 和 sensitivity_analysis.py 导入
6. 运行 py_compile 和 --help
7. 复制 SCI_paper 里的可视化源码到 scripts/visualization
8. 迁移 plot_style.py 到 src 包
9. 修正可视化脚本导入和数据路径
10. 更新 .gitignore、requirements.txt、README.md
11. 新增 docs/visualization.md 和 docs/reproduction.md
12. 检查是否误提交 npz/xlsx/svg
13. 分阶段 commit
14. git push -u origin repo-cleanup
15. GitHub 开 PR 合并
```

